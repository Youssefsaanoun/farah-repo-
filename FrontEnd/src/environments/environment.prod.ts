export const environment = {
  production: true,
  // IMPORTANT: Update this URL with your actual Render backend URL after deployment
  // The URL format should be: https://your-backend-service-name.onrender.com/api
  apiUrl: '/api'
};
